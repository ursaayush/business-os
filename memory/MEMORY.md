# Memory index

> The AI assistant's own notebook about how the owner likes to work — one file per fact, one
> pointer line here. Facts about the *business* belong in `brain/` (captured by grilling);
> this folder holds working preferences the AI learns along the way ("prefers short sessions",
> "always show the video before exporting").
>
> To make Claude Code use this folder automatically, point its memory setting
> (`autoMemoryDirectory`) here — the onboarding covers it.

_(empty — grows as the AI learns the owner's preferences)_
